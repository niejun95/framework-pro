<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.UserMapper">
    <insert id="insertNewUser" parameterType="java.util.Map">
        insert into user (name, gmt_create, gmt_modified)
        values (#{name}, #{gmtCreate}, #{gmtModified})
    </insert>

    <select id="queryUserByCreateTime" resultType="org.example.entities.AnotherUser">
        <![CDATA[ select * from user where gmt_create >= #{startDate} and gmt_create <= #{endDate}]]>
    </select>

    <select id="queryGmtCreateTime" resultType="java.sql.Date">
        select gmt_create from user where id = 3
    </select>

</mapper>
